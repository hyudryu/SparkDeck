name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    name: Backend / Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Install dependencies
        run: python -m pip install --upgrade pip pytest && python -m pip install -r requirements.txt
      - name: Run tests
        env:
          PYTHONPATH: Fan
        run: python -m pytest -q

  frontend:
    name: Frontend
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: "24"
          package-manager-cache: false
      - name: Validate frontend
        shell: bash
        run: |
          if [[ -f frontend/package-lock.json ]]; then
            cd frontend
            npm ci
            npm run lint --if-present
            npm run typecheck --if-present
            npm test
            npm run build
          elif [[ -f package-lock.json ]]; then
            npm ci
            npm run lint --if-present
            npm run typecheck --if-present
            npm test
            npm run build
          else
            echo "No npm lockfile found; frontend validation is not required yet."
          fi

  windows-launcher:
    name: Windows launcher
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install Pester
        shell: powershell
        run: Install-Module Pester -RequiredVersion 4.10.1 -Scope CurrentUser -Force -SkipPublisherCheck
      - name: Validate launcher
        shell: powershell
        run: |
          Import-Module Pester -RequiredVersion 4.10.1 -Force
          Invoke-Pester -Path tests/windows/SparkDeck.Windows.Tests.ps1 -EnableExit
